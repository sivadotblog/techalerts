---
paths:
  - "docs/**"
  - "tech-alerts/**"
  - "external-radar/**"
---

# Writing docs and alert entries

- Start each page with the Docusaurus frontmatter it expects (at minimum `title`).
- One alert/topic per file; use kebab-case filenames.
- Only edit `sidebars.ts` if a new page doesn't appear automatically.
